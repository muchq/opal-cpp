// Code generated by opal-cpp (cpp-codegen). DO NOT EDIT.

#ifndef EXAMPLE_CHAT_TYPES_H_
#define EXAMPLE_CHAT_TYPES_H_

#include <compare>
#include <cstddef>
#include <cstdint>
#include <functional>
#include <optional>
#include <ostream>
#include <string>
#include <utility>
#include <variant>
#include <vector>

#include "opal/core/fatal.h"
#include "opal/core/hash.h"
#include "opal/core/print.h"

namespace example::chat {

struct LeaveNotice {
  std::optional<std::string> reason{};

  /// Debug rendering for logs and tests — for humans, never parse it.
  void AppendDebugTo(std::string& out) const {
    out += "LeaveNotice{";
    const char* sep = "";
    if (this->reason.has_value()) {
      out += sep;
      sep = ", ";
      out += ".reason = ";
      opal::DebugAppend(out, *this->reason);
    }
    out += '}';
  }
  std::string DebugString() const { std::string out; AppendDebugTo(out); return out; }
  friend std::ostream& operator<<(std::ostream& os, const LeaveNotice& value) {
    return os << value.DebugString();
  }

  friend bool operator==(const LeaveNotice&, const LeaveNotice&) = default;
  friend auto operator<=>(const LeaveNotice&, const LeaveNotice&) = default;
};


struct ChatMessage {
  std::string text{};
  std::optional<std::string> sender{};

  /// Debug rendering for logs and tests — for humans, never parse it.
  void AppendDebugTo(std::string& out) const {
    out += "ChatMessage{";
    const char* sep = "";
    out += sep;
    sep = ", ";
    out += ".text = ";
    opal::DebugAppend(out, this->text);
    if (this->sender.has_value()) {
      out += sep;
      sep = ", ";
      out += ".sender = ";
      opal::DebugAppend(out, *this->sender);
    }
    out += '}';
  }
  std::string DebugString() const { std::string out; AppendDebugTo(out); return out; }
  friend std::ostream& operator<<(std::ostream& os, const ChatMessage& value) {
    return os << value.DebugString();
  }

  friend bool operator==(const ChatMessage&, const ChatMessage&) = default;
  friend auto operator<=>(const ChatMessage&, const ChatMessage&) = default;
};


/// What a client sends: messages, and a goodbye that asks the server to end
/// the session from its side.
class ChatEvents {
  public:
    ChatEvents() = default;

    static ChatEvents FromMessage(ChatMessage value) {
      ChatEvents result;
      result.value_.emplace<1>(std::move(value));
      return result;
    }
    bool is_message() const { return value_.index() == 1; }
    const ChatMessage& as_message() const {
      require_is(1, "message");
      return std::get<1>(value_);
    }
    /// The engaged member, or nullptr when another member (or none) is set.
    const ChatMessage* as_message_or_null() const { return std::get_if<1>(&value_); }

    static ChatEvents FromLeave(LeaveNotice value) {
      ChatEvents result;
      result.value_.emplace<2>(std::move(value));
      return result;
    }
    bool is_leave() const { return value_.index() == 2; }
    const LeaveNotice& as_leave() const {
      require_is(2, "leave");
      return std::get<2>(value_);
    }
    /// The engaged member, or nullptr when another member (or none) is set.
    const LeaveNotice* as_leave_or_null() const { return std::get_if<2>(&value_); }

    /// True until one of the From* factories has been used.
    bool empty() const { return value_.index() == 0; }

    /// Name of the engaged member, "(empty)" until a From* factory has run.
    const char* case_name() const {
      static constexpr const char* kNames[] = {"(empty)", "message", "leave"};
      return kNames[value_.index()];
    }

    /// Applies `visitor` to the engaged member. The visitor must also accept
    /// std::monostate, which represents the empty state.
    template <typename Visitor>
    decltype(auto) visit(Visitor&& visitor) const {
      return std::visit(std::forward<Visitor>(visitor), value_);
    }

    /// Debug rendering for logs and tests — for humans, never parse it.
    void AppendDebugTo(std::string& out) const {
      out += "ChatEvents(";
      switch (value_.index()) {
        case 1:
          out += "message = ";
          opal::DebugAppend(out, std::get<1>(value_));
          break;
        case 2:
          out += "leave = ";
          opal::DebugAppend(out, std::get<2>(value_));
          break;
        default:
          break;
      }
      out += ')';
    }
    std::string DebugString() const { std::string out; AppendDebugTo(out); return out; }
    friend std::ostream& operator<<(std::ostream& os, const ChatEvents& value) {
      return os << value.DebugString();
    }

    friend bool operator==(const ChatEvents&, const ChatEvents&) = default;
    friend auto operator<=>(const ChatEvents&, const ChatEvents&) = default;
    friend struct std::hash<ChatEvents>;

  private:
    void require_is(std::size_t index, const char* requested) const {
      if (value_.index() != index) {
        opal::internal::FatalWrongUnionAccess("ChatEvents", requested, case_name());
      }
    }

    std::variant<std::monostate, ChatMessage, LeaveNotice> value_;
};


struct ConverseInput {
  std::string room{};
  std::optional<std::string> nickname{};
  std::optional<ChatEvents> events{};

  /// Debug rendering for logs and tests — for humans, never parse it.
  void AppendDebugTo(std::string& out) const {
    out += "ConverseInput{";
    const char* sep = "";
    out += sep;
    sep = ", ";
    out += ".room = ";
    opal::DebugAppend(out, this->room);
    if (this->nickname.has_value()) {
      out += sep;
      sep = ", ";
      out += ".nickname = ";
      opal::DebugAppend(out, *this->nickname);
    }
    if (this->events.has_value()) {
      out += sep;
      sep = ", ";
      out += ".events = ";
      opal::DebugAppend(out, *this->events);
    }
    out += '}';
  }
  std::string DebugString() const { std::string out; AppendDebugTo(out); return out; }
  friend std::ostream& operator<<(std::ostream& os, const ConverseInput& value) {
    return os << value.DebugString();
  }

  friend bool operator==(const ConverseInput&, const ConverseInput&) = default;
  friend auto operator<=>(const ConverseInput&, const ConverseInput&) = default;
};


struct MemberJoined {
  std::string member{};

  /// Debug rendering for logs and tests — for humans, never parse it.
  void AppendDebugTo(std::string& out) const {
    out += "MemberJoined{";
    const char* sep = "";
    out += sep;
    sep = ", ";
    out += ".member = ";
    opal::DebugAppend(out, this->member);
    out += '}';
  }
  std::string DebugString() const { std::string out; AppendDebugTo(out); return out; }
  friend std::ostream& operator<<(std::ostream& os, const MemberJoined& value) {
    return os << value.DebugString();
  }

  friend bool operator==(const MemberJoined&, const MemberJoined&) = default;
  friend auto operator<=>(const MemberJoined&, const MemberJoined&) = default;
};


struct MemberLeft {
  std::string member{};

  /// Debug rendering for logs and tests — for humans, never parse it.
  void AppendDebugTo(std::string& out) const {
    out += "MemberLeft{";
    const char* sep = "";
    out += sep;
    sep = ", ";
    out += ".member = ";
    opal::DebugAppend(out, this->member);
    out += '}';
  }
  std::string DebugString() const { std::string out; AppendDebugTo(out); return out; }
  friend std::ostream& operator<<(std::ostream& os, const MemberLeft& value) {
    return os << value.DebugString();
  }

  friend bool operator==(const MemberLeft&, const MemberLeft&) = default;
  friend auto operator<=>(const MemberLeft&, const MemberLeft&) = default;
};


/// What the room sends back: broadcast messages and membership changes.
class RoomEvents {
  public:
    RoomEvents() = default;

    static RoomEvents FromMessage(ChatMessage value) {
      RoomEvents result;
      result.value_.emplace<1>(std::move(value));
      return result;
    }
    bool is_message() const { return value_.index() == 1; }
    const ChatMessage& as_message() const {
      require_is(1, "message");
      return std::get<1>(value_);
    }
    /// The engaged member, or nullptr when another member (or none) is set.
    const ChatMessage* as_message_or_null() const { return std::get_if<1>(&value_); }

    static RoomEvents FromJoined(MemberJoined value) {
      RoomEvents result;
      result.value_.emplace<2>(std::move(value));
      return result;
    }
    bool is_joined() const { return value_.index() == 2; }
    const MemberJoined& as_joined() const {
      require_is(2, "joined");
      return std::get<2>(value_);
    }
    /// The engaged member, or nullptr when another member (or none) is set.
    const MemberJoined* as_joined_or_null() const { return std::get_if<2>(&value_); }

    static RoomEvents FromLeft(MemberLeft value) {
      RoomEvents result;
      result.value_.emplace<3>(std::move(value));
      return result;
    }
    bool is_left() const { return value_.index() == 3; }
    const MemberLeft& as_left() const {
      require_is(3, "left");
      return std::get<3>(value_);
    }
    /// The engaged member, or nullptr when another member (or none) is set.
    const MemberLeft* as_left_or_null() const { return std::get_if<3>(&value_); }

    /// True until one of the From* factories has been used.
    bool empty() const { return value_.index() == 0; }

    /// Name of the engaged member, "(empty)" until a From* factory has run.
    const char* case_name() const {
      static constexpr const char* kNames[] = {"(empty)", "message", "joined", "left"};
      return kNames[value_.index()];
    }

    /// Applies `visitor` to the engaged member. The visitor must also accept
    /// std::monostate, which represents the empty state.
    template <typename Visitor>
    decltype(auto) visit(Visitor&& visitor) const {
      return std::visit(std::forward<Visitor>(visitor), value_);
    }

    /// Debug rendering for logs and tests — for humans, never parse it.
    void AppendDebugTo(std::string& out) const {
      out += "RoomEvents(";
      switch (value_.index()) {
        case 1:
          out += "message = ";
          opal::DebugAppend(out, std::get<1>(value_));
          break;
        case 2:
          out += "joined = ";
          opal::DebugAppend(out, std::get<2>(value_));
          break;
        case 3:
          out += "left = ";
          opal::DebugAppend(out, std::get<3>(value_));
          break;
        default:
          break;
      }
      out += ')';
    }
    std::string DebugString() const { std::string out; AppendDebugTo(out); return out; }
    friend std::ostream& operator<<(std::ostream& os, const RoomEvents& value) {
      return os << value.DebugString();
    }

    friend bool operator==(const RoomEvents&, const RoomEvents&) = default;
    friend auto operator<=>(const RoomEvents&, const RoomEvents&) = default;
    friend struct std::hash<RoomEvents>;

  private:
    void require_is(std::size_t index, const char* requested) const {
      if (value_.index() != index) {
        opal::internal::FatalWrongUnionAccess("RoomEvents", requested, case_name());
      }
    }

    std::variant<std::monostate, ChatMessage, MemberJoined, MemberLeft> value_;
};


struct ConverseOutput {
  std::optional<RoomEvents> events{};

  /// Debug rendering for logs and tests — for humans, never parse it.
  void AppendDebugTo(std::string& out) const {
    out += "ConverseOutput{";
    const char* sep = "";
    if (this->events.has_value()) {
      out += sep;
      sep = ", ";
      out += ".events = ";
      opal::DebugAppend(out, *this->events);
    }
    out += '}';
  }
  std::string DebugString() const { std::string out; AppendDebugTo(out); return out; }
  friend std::ostream& operator<<(std::ostream& os, const ConverseOutput& value) {
    return os << value.DebugString();
  }

  friend bool operator==(const ConverseOutput&, const ConverseOutput&) = default;
  friend auto operator<=>(const ConverseOutput&, const ConverseOutput&) = default;
};


/// The moderator ended the session mid-stream. Surfaces on the client as the
/// unary error shape: kind kModeled, code "Kicked", typed detail attached.
struct Kicked {
  std::optional<std::string> message{};
  std::string by{};

  /// Debug rendering for logs and tests — for humans, never parse it.
  void AppendDebugTo(std::string& out) const {
    out += "Kicked{";
    const char* sep = "";
    if (this->message.has_value()) {
      out += sep;
      sep = ", ";
      out += ".message = ";
      opal::DebugAppend(out, *this->message);
    }
    out += sep;
    sep = ", ";
    out += ".by = ";
    opal::DebugAppend(out, this->by);
    out += '}';
  }
  std::string DebugString() const { std::string out; AppendDebugTo(out); return out; }
  friend std::ostream& operator<<(std::ostream& os, const Kicked& value) {
    return os << value.DebugString();
  }

  friend bool operator==(const Kicked&, const Kicked&) = default;
  friend auto operator<=>(const Kicked&, const Kicked&) = default;
};


struct ListRoomsInput {
  /// Debug rendering for logs and tests — for humans, never parse it.
  void AppendDebugTo(std::string& out) const {
    out += "ListRoomsInput{";
    out += '}';
  }
  std::string DebugString() const { std::string out; AppendDebugTo(out); return out; }
  friend std::ostream& operator<<(std::ostream& os, const ListRoomsInput& value) {
    return os << value.DebugString();
  }

  friend bool operator==(const ListRoomsInput&, const ListRoomsInput&) = default;
  friend auto operator<=>(const ListRoomsInput&, const ListRoomsInput&) = default;
};


struct RoomSummary {
  std::string name{};
  std::int32_t members{};

  /// Debug rendering for logs and tests — for humans, never parse it.
  void AppendDebugTo(std::string& out) const {
    out += "RoomSummary{";
    const char* sep = "";
    out += sep;
    sep = ", ";
    out += ".name = ";
    opal::DebugAppend(out, this->name);
    out += sep;
    sep = ", ";
    out += ".members = ";
    opal::DebugAppend(out, this->members);
    out += '}';
  }
  std::string DebugString() const { std::string out; AppendDebugTo(out); return out; }
  friend std::ostream& operator<<(std::ostream& os, const RoomSummary& value) {
    return os << value.DebugString();
  }

  friend bool operator==(const RoomSummary&, const RoomSummary&) = default;
  friend auto operator<=>(const RoomSummary&, const RoomSummary&) = default;
};


struct ListRoomsOutput {
  std::vector<RoomSummary> rooms{};

  /// Debug rendering for logs and tests — for humans, never parse it.
  void AppendDebugTo(std::string& out) const {
    out += "ListRoomsOutput{";
    const char* sep = "";
    out += sep;
    sep = ", ";
    out += ".rooms = ";
    opal::DebugAppend(out, this->rooms);
    out += '}';
  }
  std::string DebugString() const { std::string out; AppendDebugTo(out); return out; }
  friend std::ostream& operator<<(std::ostream& os, const ListRoomsOutput& value) {
    return os << value.DebugString();
  }

  friend bool operator==(const ListRoomsOutput&, const ListRoomsOutput&) = default;
  friend auto operator<=>(const ListRoomsOutput&, const ListRoomsOutput&) = default;
};


struct WatchInput {
  std::string room{};

  /// Debug rendering for logs and tests — for humans, never parse it.
  void AppendDebugTo(std::string& out) const {
    out += "WatchInput{";
    const char* sep = "";
    out += sep;
    sep = ", ";
    out += ".room = ";
    opal::DebugAppend(out, this->room);
    out += '}';
  }
  std::string DebugString() const { std::string out; AppendDebugTo(out); return out; }
  friend std::ostream& operator<<(std::ostream& os, const WatchInput& value) {
    return os << value.DebugString();
  }

  friend bool operator==(const WatchInput&, const WatchInput&) = default;
  friend auto operator<=>(const WatchInput&, const WatchInput&) = default;
};


struct WatchOutput {
  std::optional<RoomEvents> events{};

  /// Debug rendering for logs and tests — for humans, never parse it.
  void AppendDebugTo(std::string& out) const {
    out += "WatchOutput{";
    const char* sep = "";
    if (this->events.has_value()) {
      out += sep;
      sep = ", ";
      out += ".events = ";
      opal::DebugAppend(out, *this->events);
    }
    out += '}';
  }
  std::string DebugString() const { std::string out; AppendDebugTo(out); return out; }
  friend std::ostream& operator<<(std::ostream& os, const WatchOutput& value) {
    return os << value.DebugString();
  }

  friend bool operator==(const WatchOutput&, const WatchOutput&) = default;
  friend auto operator<=>(const WatchOutput&, const WatchOutput&) = default;
};

}  // namespace example::chat

// std::hash so generated types key std::unordered_map/std::unordered_set —
// emitted exactly for the types that get operator<=> (issue #49). Hash
// values are process-local: never persist or compare them across runs.

template <>
struct std::hash<example::chat::LeaveNotice> {
  std::size_t operator()(const example::chat::LeaveNotice& value) const noexcept {
    std::size_t seed = 0;
    seed = opal::HashCombine(seed, opal::HashValue(value.reason));
    return seed;
  }
};

template <>
struct std::hash<example::chat::ChatMessage> {
  std::size_t operator()(const example::chat::ChatMessage& value) const noexcept {
    std::size_t seed = 0;
    seed = opal::HashCombine(seed, opal::HashValue(value.text));
    seed = opal::HashCombine(seed, opal::HashValue(value.sender));
    return seed;
  }
};

template <>
struct std::hash<example::chat::ChatEvents> {
  std::size_t operator()(const example::chat::ChatEvents& value) const noexcept {
    const std::size_t member =
        std::visit([](const auto& v) { return opal::HashValue(v); }, value.value_);
    return opal::HashCombine(value.value_.index(), member);
  }
};

template <>
struct std::hash<example::chat::ConverseInput> {
  std::size_t operator()(const example::chat::ConverseInput& value) const noexcept {
    std::size_t seed = 0;
    seed = opal::HashCombine(seed, opal::HashValue(value.room));
    seed = opal::HashCombine(seed, opal::HashValue(value.nickname));
    seed = opal::HashCombine(seed, opal::HashValue(value.events));
    return seed;
  }
};

template <>
struct std::hash<example::chat::MemberJoined> {
  std::size_t operator()(const example::chat::MemberJoined& value) const noexcept {
    std::size_t seed = 0;
    seed = opal::HashCombine(seed, opal::HashValue(value.member));
    return seed;
  }
};

template <>
struct std::hash<example::chat::MemberLeft> {
  std::size_t operator()(const example::chat::MemberLeft& value) const noexcept {
    std::size_t seed = 0;
    seed = opal::HashCombine(seed, opal::HashValue(value.member));
    return seed;
  }
};

template <>
struct std::hash<example::chat::RoomEvents> {
  std::size_t operator()(const example::chat::RoomEvents& value) const noexcept {
    const std::size_t member =
        std::visit([](const auto& v) { return opal::HashValue(v); }, value.value_);
    return opal::HashCombine(value.value_.index(), member);
  }
};

template <>
struct std::hash<example::chat::ConverseOutput> {
  std::size_t operator()(const example::chat::ConverseOutput& value) const noexcept {
    std::size_t seed = 0;
    seed = opal::HashCombine(seed, opal::HashValue(value.events));
    return seed;
  }
};

template <>
struct std::hash<example::chat::Kicked> {
  std::size_t operator()(const example::chat::Kicked& value) const noexcept {
    std::size_t seed = 0;
    seed = opal::HashCombine(seed, opal::HashValue(value.message));
    seed = opal::HashCombine(seed, opal::HashValue(value.by));
    return seed;
  }
};

template <>
struct std::hash<example::chat::ListRoomsInput> {
  std::size_t operator()(const example::chat::ListRoomsInput& /*value*/) const noexcept { return 0; }
};

template <>
struct std::hash<example::chat::RoomSummary> {
  std::size_t operator()(const example::chat::RoomSummary& value) const noexcept {
    std::size_t seed = 0;
    seed = opal::HashCombine(seed, opal::HashValue(value.name));
    seed = opal::HashCombine(seed, opal::HashValue(value.members));
    return seed;
  }
};

template <>
struct std::hash<example::chat::ListRoomsOutput> {
  std::size_t operator()(const example::chat::ListRoomsOutput& value) const noexcept {
    std::size_t seed = 0;
    seed = opal::HashCombine(seed, opal::HashValue(value.rooms));
    return seed;
  }
};

template <>
struct std::hash<example::chat::WatchInput> {
  std::size_t operator()(const example::chat::WatchInput& value) const noexcept {
    std::size_t seed = 0;
    seed = opal::HashCombine(seed, opal::HashValue(value.room));
    return seed;
  }
};

template <>
struct std::hash<example::chat::WatchOutput> {
  std::size_t operator()(const example::chat::WatchOutput& value) const noexcept {
    std::size_t seed = 0;
    seed = opal::HashCombine(seed, opal::HashValue(value.events));
    return seed;
  }
};

#endif  // EXAMPLE_CHAT_TYPES_H_
