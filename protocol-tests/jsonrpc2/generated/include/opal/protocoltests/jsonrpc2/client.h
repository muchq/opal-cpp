// Code generated by smithy-cpp (cpp-codegen). DO NOT EDIT.

#ifndef OPAL_PROTOCOLTESTS_JSONRPC2_CLIENT_H_
#define OPAL_PROTOCOLTESTS_JSONRPC2_CLIENT_H_

#include <compare>
#include <cstddef>
#include <functional>
#include <memory>
#include <ostream>
#include <string>
#include <utility>
#include <variant>

#include "opal/protocoltests/jsonrpc2/types.h"
#include "smithy/client/config.h"
#include "smithy/core/fatal.h"
#include "smithy/core/hash.h"
#include "smithy/core/outcome.h"
#include "smithy/core/print.h"
#include "smithy/eventstream/event_stream.h"
#include "smithy/http/transport.h"

namespace opal::protocoltests::jsonrpc2 {

/// The typed session EchoStream returns (ADR-0016): Tx = what this client
/// sends, Rx = what the server sends.
using EchoStreamClientStream = opal::eventstream::EventStream<UpEvents, DownEvents>;

/// jsonRpc2 client for smithy.cpp.protocoltests.jsonrpc2#JsonRpc2Protocol.
/// Modeled service errors surface as opal::Error with kind kModeled,
/// code() set to the error shape name, and the deserialized error
/// structure attached. Dispatch on them through the per-operation
/// <Operation>Errors listings below rather than comparing code() text.
class JsonRpc2ProtocolClient {
  public:
    /// Fails when the endpoint cannot be parsed and no transport is injected.
    static opal::Outcome<JsonRpc2ProtocolClient> Create(opal::ClientConfig config);

    opal::Outcome<EchoPayloadOutput> EchoPayload(const EchoPayloadInput& input) const;
    /// The stream wire's conformance surface (ADR-0023). The smithy test traits
    /// are request/response-shaped and cannot express a stream, so the stream
    /// cases are authored as C++ in ../stream_conformance_test.cc — normative
    /// for the envelopes (opening call, notification events with the id echoed
    /// inside params, terminal result/error, the reserved codes) the way the
    /// trait cases above are for the unary wire.
    ///
    /// Opens the operation's event stream over a WebSocket upgrade
    /// (ADR-0016). Send carries input events, Receive yields output events
    /// (nullopt on the peer's clean close), and a received exception
    /// surfaces through Receive() as a modeled error, the unary shape.
    opal::Outcome<EchoStreamClientStream> EchoStream(const EchoStreamInput& input) const;
    opal::Outcome<NoArgsOutput> NoArgs(const NoArgsInput& input = {}) const;
    opal::Outcome<PutConstrainedOutput> PutConstrained(const PutConstrainedInput& input) const;

  private:
    JsonRpc2ProtocolClient(opal::ClientConfig config, std::shared_ptr<opal::http::HttpClient> transport, std::string path_prefix);
    opal::Outcome<opal::http::HttpResponse> Send(opal::http::HttpRequest request) const;

    opal::ClientConfig config_;
    std::shared_ptr<opal::http::HttpClient> transport_;
    std::string path_prefix_;
};

/// The modeled errors of EchoPayload, matched from a opal::Error so dispatch is
/// typed and exhaustive instead of string-compared. FromError() is empty()
/// when the error is none of this operation's modeled errors (transport,
/// serialization, unknown, or another operation's error).
class EchoPayloadErrors {
  public:
    EchoPayloadErrors() = default;

    /// Matches `error` against this operation's modeled errors. An engaged
    /// member carries the deserialized error detail, default-initialized when
    /// the error arrived without one.
    static EchoPayloadErrors FromError(const opal::Error& error) {
      EchoPayloadErrors result;
      if (error.kind() != opal::ErrorKind::kModeled) return result;
      if (error.code() == "NotFoundError") {
        const auto* detail = error.detail<NotFoundError>();
        result.value_.emplace<1>(detail != nullptr ? *detail : NotFoundError{});
        return result;
      }
      if (error.code() == "ThrottledError") {
        const auto* detail = error.detail<ThrottledError>();
        result.value_.emplace<2>(detail != nullptr ? *detail : ThrottledError{});
        return result;
      }
      return result;
    }

    bool is_not_found_error() const { return value_.index() == 1; }
    const NotFoundError& as_not_found_error() const {
      require_is(1, "not_found_error");
      return std::get<1>(value_);
    }
    /// The engaged member, or nullptr when another member (or none) is set.
    const NotFoundError* as_not_found_error_or_null() const { return std::get_if<1>(&value_); }

    bool is_throttled_error() const { return value_.index() == 2; }
    const ThrottledError& as_throttled_error() const {
      require_is(2, "throttled_error");
      return std::get<2>(value_);
    }
    /// The engaged member, or nullptr when another member (or none) is set.
    const ThrottledError* as_throttled_error_or_null() const { return std::get_if<2>(&value_); }

    /// True when the error is none of this operation's modeled errors.
    bool empty() const { return value_.index() == 0; }

    /// Name of the engaged member, "(empty)" when none matched.
    const char* case_name() const {
      static constexpr const char* kNames[] = {"(empty)", "not_found_error", "throttled_error"};
      return kNames[value_.index()];
    }

    /// Applies `visitor` to the engaged member. The visitor must also accept
    /// std::monostate, which represents the empty state.
    template <typename Visitor>
    decltype(auto) visit(Visitor&& visitor) const {
      return std::visit(std::forward<Visitor>(visitor), value_);
    }

    /// Debug rendering for logs and tests — for humans, never parse it.
    void AppendDebugTo(std::string& out) const {
      out += "EchoPayloadErrors(";
      switch (value_.index()) {
        case 1:
          out += "not_found_error = ";
          opal::DebugAppend(out, std::get<1>(value_));
          break;
        case 2:
          out += "throttled_error = ";
          opal::DebugAppend(out, std::get<2>(value_));
          break;
        default:
          break;
      }
      out += ')';
    }
    std::string DebugString() const { std::string out; AppendDebugTo(out); return out; }
    friend std::ostream& operator<<(std::ostream& os, const EchoPayloadErrors& value) {
      return os << value.DebugString();
    }

    friend bool operator==(const EchoPayloadErrors&, const EchoPayloadErrors&) = default;
    friend auto operator<=>(const EchoPayloadErrors&, const EchoPayloadErrors&) = default;
    friend struct std::hash<EchoPayloadErrors>;

  private:
    void require_is(std::size_t index, const char* requested) const {
      if (value_.index() != index) {
        opal::internal::FatalWrongUnionAccess("EchoPayloadErrors", requested, case_name());
      }
    }

    std::variant<std::monostate, NotFoundError, ThrottledError> value_;
};

/// The modeled errors of EchoStream, matched from a opal::Error so dispatch is
/// typed and exhaustive instead of string-compared. FromError() is empty()
/// when the error is none of this operation's modeled errors (transport,
/// serialization, unknown, or another operation's error).
class EchoStreamErrors {
  public:
    EchoStreamErrors() = default;

    /// Matches `error` against this operation's modeled errors. An engaged
    /// member carries the deserialized error detail, default-initialized when
    /// the error arrived without one.
    static EchoStreamErrors FromError(const opal::Error& error) {
      EchoStreamErrors result;
      if (error.kind() != opal::ErrorKind::kModeled) return result;
      if (error.code() == "StreamAbort") {
        const auto* detail = error.detail<StreamAbort>();
        result.value_.emplace<1>(detail != nullptr ? *detail : StreamAbort{});
        return result;
      }
      return result;
    }

    bool is_stream_abort() const { return value_.index() == 1; }
    const StreamAbort& as_stream_abort() const {
      require_is(1, "stream_abort");
      return std::get<1>(value_);
    }
    /// The engaged member, or nullptr when another member (or none) is set.
    const StreamAbort* as_stream_abort_or_null() const { return std::get_if<1>(&value_); }

    /// True when the error is none of this operation's modeled errors.
    bool empty() const { return value_.index() == 0; }

    /// Name of the engaged member, "(empty)" when none matched.
    const char* case_name() const {
      static constexpr const char* kNames[] = {"(empty)", "stream_abort"};
      return kNames[value_.index()];
    }

    /// Applies `visitor` to the engaged member. The visitor must also accept
    /// std::monostate, which represents the empty state.
    template <typename Visitor>
    decltype(auto) visit(Visitor&& visitor) const {
      return std::visit(std::forward<Visitor>(visitor), value_);
    }

    /// Debug rendering for logs and tests — for humans, never parse it.
    void AppendDebugTo(std::string& out) const {
      out += "EchoStreamErrors(";
      switch (value_.index()) {
        case 1:
          out += "stream_abort = ";
          opal::DebugAppend(out, std::get<1>(value_));
          break;
        default:
          break;
      }
      out += ')';
    }
    std::string DebugString() const { std::string out; AppendDebugTo(out); return out; }
    friend std::ostream& operator<<(std::ostream& os, const EchoStreamErrors& value) {
      return os << value.DebugString();
    }

    friend bool operator==(const EchoStreamErrors&, const EchoStreamErrors&) = default;
    friend auto operator<=>(const EchoStreamErrors&, const EchoStreamErrors&) = default;
    friend struct std::hash<EchoStreamErrors>;

  private:
    void require_is(std::size_t index, const char* requested) const {
      if (value_.index() != index) {
        opal::internal::FatalWrongUnionAccess("EchoStreamErrors", requested, case_name());
      }
    }

    std::variant<std::monostate, StreamAbort> value_;
};

}  // namespace opal::protocoltests::jsonrpc2

// std::hash so generated types key std::unordered_map/std::unordered_set —
// emitted exactly for the types that get operator<=> (issue #49). Hash
// values are process-local: never persist or compare them across runs.

template <>
struct std::hash<opal::protocoltests::jsonrpc2::EchoPayloadErrors> {
  std::size_t operator()(const opal::protocoltests::jsonrpc2::EchoPayloadErrors& value) const noexcept {
    const std::size_t member =
        std::visit([](const auto& v) { return opal::HashValue(v); }, value.value_);
    return opal::HashCombine(value.value_.index(), member);
  }
};

template <>
struct std::hash<opal::protocoltests::jsonrpc2::EchoStreamErrors> {
  std::size_t operator()(const opal::protocoltests::jsonrpc2::EchoStreamErrors& value) const noexcept {
    const std::size_t member =
        std::visit([](const auto& v) { return opal::HashValue(v); }, value.value_);
    return opal::HashCombine(value.value_.index(), member);
  }
};

#endif  // OPAL_PROTOCOLTESTS_JSONRPC2_CLIENT_H_
